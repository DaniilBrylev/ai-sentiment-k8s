apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sentiment-api
  namespace: ai-demo
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: sentiment-api
                port:
                  number: 80